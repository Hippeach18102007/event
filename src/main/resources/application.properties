spring.application.name=event

# Railway/Render dùng PORT env, fallback 8080
server.port=${PORT:8080}

# Resend (HTTP API)
resend.api-key=${RESEND_API_KEY}
mail.from=${MAIL_FROM:onboarding@resend.dev}
mail.from-name=${MAIL_FROM_NAME:Event App}
